# Runtime dependencies (host + client)
dxcam==0.0.5
av==13.1.0
PyQt6==6.7.1
pystray==0.19.5
Pillow==10.4.0
pywin32==306
cryptography==43.0.1

# Build / packaging (dev only)
pyinstaller==6.10.0

# Testing (dev only)
pytest==8.3.3
pytest-asyncio==0.24.0
